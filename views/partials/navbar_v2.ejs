<nav class="navbar navbar-expand-lg navbar-light bg-transparent">
    <div class="container-fluid">
        <button type="button" id="sidebarCollapse" class="btn bg-transparent">
            <i class="fas fa-cog" style="color: #f1af20;"></i>
        </button>

        <div class="row row-flex">
            <h1 class="title mr-2 ml-3">EggBook</h1>
            <span class="fa-stack" style="vertical-align: top;">
                <i class="fas fa-egg fa-stack-1x" style="color: #e4e9f4; font-size: 2.2em;"></i>
                <i class="fas fa-egg fa-stack-1x fa-inverse" style="color: #f1af20; font-size: 1.5em;"></i>
            </span>
        </div>

        <button type="button" class="navbar-toggler mb-3" data-toggle="collapse" data-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <div class="container-fluid">
                <div class="input-group search-style">
                    <input list="searchHistory" id=searchBox value="" class="form-control ml-5"
                        placeholder="Search for your favorite book here!">
                    <datalist id="searchHistory">
                        <% if (locals.user) { %>
                            <option id="History1"><%= user.search_history[0]%> </option>
                            <option id="History2"><%= user.search_history[1]%></option>
                            <option id="History3"><%= user.search_history[2]%></option>
                        <% } else { %> 
                            <h3>Log-in to save searches</h3>
                        <% } %> 
                    </datalist>
                    <div class="input-group-append">
                        <button onclick=SaveSearch_RedirectSearch() class="btn btn-secondary mr-5" type="button">
                            <i class="fa fa-search"></i>
                        </button>
                    </div>
                </div>
            </div>
            <div class="navbar-nav navbar-light ml-auto">
                <a href="/" class="nav-item nav-link active mx-auto">HOME</a>
                <a href="/bookmarks" class="nav-item nav-link mx-auto">BOOKMARKS</a>
                <a href="/owned" class="nav-item nav-link mx-auto">OWNED</a>
                <a href="/cart" class="nav-item nav-link mx-auto"> <i class="fas fa-shopping-cart"></i> </a>
            </div>
        </div>
    </div>
</nav>